:css
  .overlay 
  {
    background: #4C95AF;
    background:
        -moz-linear-gradient(
            center bottom,
            rgba(76,149,175,0.1) 5%,
            rgba(183,185,52,0.9) 95%
        );
    background: -webkit-gradient(
        linear,
        left bottom,
        left top,
        color-stop(0.05, rgba(76,149,175,0.1)),
        color-stop(0.95, rgba(183,185,52,0.9))
    );
  }

%article.thirteen.columns.alpha
  %section
    Unlike the famous smoggy pollution of old foggy London, today pollution is invisible but just as real. This website uses real time data from 
    %a(href="http://www.londonair.org.uk/LondonAir/Default.aspx") the London Air project
    and Google street view
    to help you see the pollution that is present but invisible.

    %h2=site.attributes["sitename"]

    #container{:style => "position: relative; width: 500px; height: 300px;"}
      #pano{:name => "pano", :style => "width: 100%; height: 100%;z:index: 0"}
      %iframe.overlay{:frameborder => "0", :src => "javascript:false;", :style => "position: absolute; right: 185px; top: 5px; width: 150px; height: 55px; z-index: 1;"}
      %div{:style => "position: absolute; right: 185px; top: 5px; width: 150px;height: 55px; z-index: 2;"}
        / %img{:src => "http://www.google.com/intl/en_uk/websiteoptimizer/images/logo_sm.gif"}/

%section.three.columns.omega
  %aside.stats
    %table
      %tr 
        %th(scope="row") Maybe the scale here?
        %td


- content_for :javascript do
  :javascript
    function initialize() {
      var myPano = new GStreetviewPanorama(document.getElementById("pano"));
      fenwayPark = new GLatLng(#{site.attributes["latitude"]},#{site.attributes["longitude"]});
      myPOV = {yaw:0,pitch:0};
      myPano.setLocationAndPOV(fenwayPark, myPOV);
    }

    /* 
    function initialize() {
      var draggable = true;
      if(navigator.userAgent.match(/iPhone/i)){
        draggable = false;
      }
      var myOptions = {
        scrollwheel: false,
        draggable: draggable,
        zoom: 13,
        mapTypeId: google.maps.MapTypeId.HYBRID
      };
      var map = new google.maps.Map(document.getElementById("map_canvas"),
          myOptions);
          var geoLayer = new google.maps.KmlLayer('http://saxon-switzerland.heroku.com/geo-data/test.kmz');
          geoLayer.setMap(map);
    }
  
    google.maps.event.addDomListener(window, 'load', initialize);
  
    (function(window, $, PhotoSwipe){
      $(document).ready(function(){
        var options = {};
        $("#Gallery a").photoSwipe(options);
      });
    }(window, window.jQuery, window.Code.PhotoSwipe));
    */